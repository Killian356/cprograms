#include <stdio.h>
//Adding  Multiple defined floating points
int main()
{
    float x = 0.1,
    y = 0.1,
    z = 0.1;

    float total = x + y + z;

    printf("The sum is %.20f\n", total);
    return 0;
}